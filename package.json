{
  "name": "forem-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "Remote MCP server for managing Forem posts on Vercel.",
  "type": "module",
  "scripts": {
    "start": "vercel dev",
    "check:conflicts": "if rg -n '^(<<<<<<<|=======|>>>>>>>)' api README.md vercel.json package.json; then exit 1; else exit 0; fi",
    "check": "npm run check:conflicts && node --check api/index.js"
  }
}
